{
  "name": "codex-pocket",
  "version": "0.1.0",
  "private": true,
  "description": "A mobile-first, local-only web broker for Codex app-server.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "check": "node --check src/server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0",
    "qrcode": "^1.5.4",
    "ws": "^8.18.2"
  }
}
